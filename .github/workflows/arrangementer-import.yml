name: Arrangementer import

on:
  workflow_dispatch:
  schedule:
    - cron: "17 */2 * * *"

permissions:
  contents: write

concurrency:
  group: arrangementer-import
  cancel-in-progress: false

jobs:
  import:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Import arrangementer
        env:
          ARRANGEMENT_BASEROW_TOKEN: ${{ secrets.ARRANGEMENT_BASEROW_TOKEN }}
          ARRANGEMENT_BASEROW_TOKEN_SANDNES: ${{ secrets.ARRANGEMENT_BASEROW_TOKEN_SANDNES }}
          BASEROW_API_BASE: https://api.baserow.io
          ARRANGEMENT_DATA_PATH: arrangementer-data.json
        run: node scripts/run-arrangementer-import.mjs

      - name: Commit oppdatert snapshot
        run: |
          # git diff ser ikke nye, untracked filer.
          # git status --porcelain gjør det.
          if [ -z "$(git status --porcelain -- arrangementer-data.json)" ]; then
            echo "Ingen endringer i arrangementer-data.json"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add arrangementer-data.json
          git commit -m "Update arrangementer data"
          git push
